name: any_image
description: "A universal Flutter image widget that renders any source"
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  cached_network_image: ^3.4.1
  flutter_svg: ^2.2.4

flutter:
  
